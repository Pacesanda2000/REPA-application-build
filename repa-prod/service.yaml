apiVersion: v1
kind: Service
metadata:
  name: repa-app
  labels:
    app: repa-app
  namespace: prod
spec:
   selector:
     app: repa-app
   ports:
   - protocol: TCP
     port: 80
     targetPort: 80
