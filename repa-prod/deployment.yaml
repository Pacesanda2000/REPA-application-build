apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: repa-app
  name: repa-deployment
  namespace: prod
spec:
  selector:
    matchLabels:
      app: repa-app
  template:
    metadata:
      labels:
        app: repa-app
    spec:
      nodeSelector: 
        nodetype: worker 
      containers:
      - image: dmajoros/repa:latest
        name: repa-app
        resources: 
          limits:
            cpu: 170m
          requests:
            cpu: 80m